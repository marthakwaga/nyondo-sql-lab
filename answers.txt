with Template literals, data is displayed the way it is input which is risky.The database reads the input as part of the command. With a parameterized () query, the database is told exactly what the "command" is first, and the input is treated strictly as text (data) that cannot change the command's logic.